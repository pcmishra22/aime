= simple_form_for(@user) do |f|
  = f.error_notification

  .form-group
    .form-inputs
      = f.input :first_name,:label=>'First Name' ,:input_html=>{ :class=>'form-control' }

  .form-group
    .form-inputs
      = f.input :last_name,:label=>'Last Name' ,:input_html=>{ :class=>'form-control' }

  .form-group
    .form-inputs
      = f.input :email,:label=>'Email' ,:input_html=>{ :class=>'form-control' }

  .form-group
    .form-inputs
      = f.input :password,:label=>'Password' ,:input_html=>{ :class=>'form-control' }

  .form-group
    .form-inputs
      = f.input :password_confirmation,:label=>'Confirm Password' ,:input_html=>{ :class=>'form-control' }
  .form-group
    .form-inputs
      = f.input :type,
        collection: [['Student', 'Student'],['Teacher','Teacher'],['Parent','Parent']],
        label_method: :last,
        value_method: :first,
        as: :radio_buttons,
        checked: 'Student',
        required: true
  .form-actions
    = f.button :submit, "Submit",:class=>'btn btn-primary'