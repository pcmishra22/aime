.col-lg-12
- @users.each do |user|	
	.col-lg-3.col-md-4.col-xs-6.thumb
		%a{href:"/users/profile/#{ user.id} ",class:"thumbnail"}
			= image_tag ("#{ user.profile_photo || ('defaultm.png')}"),class:"img-responsive", height:"100px",width:"100px"
			%br
			#{ user.first_name} #{ user.last_name} , #{ user.usertype} 
			%br
.clearfix