%section#mu-gallery
  .container
    .row
      .col-md-12
        = link_to "Add Schedule", new_teacher_course_schedule_path(course_id: @course_id)
    .row
      .col-md-3
        %strong Topic
      .col-md-3
        %strong Start Time
      .col-md-3
        %strong End Time
      .col-md-3
        %strong Action

      - @schedule.each do |schedule|
        .row
          .col-md-3.m-t-10
            = schedule.topic
          .col-md-3.m-t-10
            = schedule.startdate.strftime("%d-%m-%Y ")
            = schedule.starttime.strftime("%H:%M")
          .col-md-3.m-t-10
            = schedule.enddate.strftime("%d-%m-%Y ")
            = schedule.endtime.strftime("%H:%M")
          .col-md-3.m-t-10
            = link_to "Edit" ,edit_teacher_course_schedule_path(schedule,:course_id=>schedule.course_id)
